# AeroGuard-MAS GUI

This folder contains the Streamlit replay visualizer for AeroGuard-MAS JSONL simulation events.

## Run

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
streamlit run app.py
```
